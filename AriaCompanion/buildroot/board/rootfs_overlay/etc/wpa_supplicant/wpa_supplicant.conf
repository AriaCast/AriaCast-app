ctrl_interface=/var/run/wpa_supplicant
ctrl_interface_group=0
update_config=1
country=IT

# No network configured — device will boot into AP setup mode.
# aria-mgr writes credentials here when you submit the captive portal form.
